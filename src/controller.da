import random

class Controller(process):

    def setup(nprocs, processes, lambda_t):

        self.ps = processes

    def run():

        proposerSet = random.sample(ps, nprocs/2)
        send(('Propose',), to=proposerSet)

        if await(False):
            pass
        elif timeout(lambda_t):
            pass
